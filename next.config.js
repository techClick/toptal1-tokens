/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    'https://*.replit.dev',
    'https://*.replit.app',
    'https://*.janeway.replit.dev'
  ],
}

module.exports = nextConfig
